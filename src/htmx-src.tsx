/**
 * An `<script>` tag that contains the htmx source code. `htmx.org` package
 * must be installed, otherwise this will return an empty `<script>` tag.
 */
declare const HtmxScript: () => JSX.Element;

export { HtmxScript };
